module.exports = {
    toggleLogin:  (display) => {
        return !display
    }, 

    toggleRedirect: (redirect) => {
        return !redirect
    }, 

    handleChange:  (name, value) => {
            return name = value
    }
}